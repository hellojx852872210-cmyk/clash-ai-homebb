[project]
name = "clash-ai-homebb"
version = "0.2.0"
description = "AI 流量只走家宽、其它走任何能通的订阅：Clash Verge Rev 死链生成器 + macOS 监控/悬浮窗/配置锁"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.optional-dependencies]
hud = ["pyobjc-framework-Cocoa"]
dev = ["pytest", "pyyaml"]

[tool.pytest.ini_options]
testpaths = ["tests"]
